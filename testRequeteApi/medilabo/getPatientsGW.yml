info:
  name: getPatientsGW
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/api/v1/patient/list
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
